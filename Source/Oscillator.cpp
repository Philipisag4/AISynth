#include "Oscillator.h"

namespace aisynth {
// All logic is header-only for inlining; this translation unit exists so the
// CMake target has a concrete source to compile (and to keep the file list
// uniform across the project).
} // namespace aisynth
